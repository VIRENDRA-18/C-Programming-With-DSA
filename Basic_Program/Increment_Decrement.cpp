#include<iostream>
using namespace std;
int main() {
    int x = 10;
    x++; // First use the current value, then increase it by 1.
    
    ++x; // First increase the value, then use the new value.
    x--;
    --x;
    cout<<x<<endl;
    cout<<x;
}

